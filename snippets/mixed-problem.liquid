{%
  render 'radio-fields',
  title:"The glaze made with water",
  field_id:"unpackedSogoWithWaterProblem",
  required: true,
  field_name:"mixed_sogo",
  field_values:"
  unpackedSogoMixedProblemThick|Is too thick|unpackedSogoThickMixtureSolute,
  unpackedSogoMixedProblemRunny|Is too runny,
  unpackedSogoMixedProblemHard|Has become rock hard,
  unpackedSogoMixedProblemPebbles|contains granules/pebbles,
  unpackedSogoMixedProblemPebbles|Is very powdery after glazing,
  unpackedSogoMixedProblemPebbles|There is nothing else wrong,
  unpackedSogoMixedProblemElse|Something else
  ",
  section_id:{{section_id }}
%}

{% render 'thick-mixture', section_id: section_id %}
{% comment %}
  {% render 'pebble-brush', section_id: section_id %}

  {% render 'hard-brush', section_id: section_id %}

  {% render 'runny-brush', section_id: section_id %}
{% endcomment %}
