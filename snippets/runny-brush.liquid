{% comment %} unpackedSogoBrushSolute {% endcomment %}

{%
  render 'radio-fields',
  title:"Did you prepare the glaze with diluted pehatine according to the instruction on the package?",
  field_id:"unpackedSogoRunnyBrushSolute",
  required: true,
  field_name:"unbacked_sogo_runny_brush_solute",
  field_values:"
  unpackedSogoBrushRunnySoluteYes|Yes,
  unpackedSogoBrushRunnySoluteNo|No
  ",
  section_id:{{section_id }}
%}

{%
  render 'radio-fields',
  title:"Have you ever measured the specific gravity",
  field_id:"unpackedSogoRunnyBrushGravityYes",
  required: true,
  field_name:"unbacked_sogo_runny_brush_gravity",
  field_values:"
  unpackedSogoBrushRunnyYesMatchesGravity|Yes it matches the packaging,
  unpackedSogoBrushRunnyYesLowerGravity|Yes but it is lower on the packaging,
  unpackedSogoBrushRunnyNoGravity|No
  ",
  section_id:{{section_id }}
%}

{%
  render 'radio-fields',
  instruction:"Diluted pehatine has a limited shelf life. It can lose its effectiveness if left for too long. Your glaze may have become too runny because the pehatine has spoiled. Add a dash of undiluted pehatine to the glaze to thicken it again.",
  field_id:"unpackedSogoRunnyBrushSoluteNoInstruction",
  section_id:{{section_id }}
%}
{%
  render 'radio-fields',
  instruction:"Diluted pehatine has a limited shelf life. It can lose its effectiveness if left for too long. Your glaze may have become too runny because the pehatine has spoiled. Add a dash of undiluted pehatine to the glaze to thicken it again.",
  field_id:"unpackedSogoRunnyBrushYesGravityMatchesInstruction",
  section_id:{{section_id }}
%}
{%
  render 'radio-fields',
  instruction:"If the specific gravity of your brush glaze is lower than the one indicated on the package, there is too much diluted pehatine solution in the glaze. You'll need to remove some of the pehatine water from the glaze (using a syringe, for example). Stir the glaze again and check the specific gravity until it matches the one on the package.",
  field_id:"unpackedSogoRunnyBrushYesGravityLowerInstruction",
  section_id:{{section_id }}
%}
{%
  render 'radio-fields',
  instruction:"Measure the specific gravity and compare it to the specific gravity on the packaging. Go to the previous question and adjust your answer accordingly.",
  field_id:"unpackedSogoRunnyBrushNoGravityInstruction",
  section_id:{{section_id }}
%}
